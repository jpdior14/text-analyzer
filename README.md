# Simple Text Analyzer

A command-line tool written in C++ that analyzes a text file and reports on the number of lines, words, and characters.

This project serves as a demonstration of taking ownership of an inherited, bug-ridden codebase and professionalizing it.

## Building
To build the executable, run the 'make' command:

## Usage
To analyze a file, provide the filename as a command-line argument:

Example:
